name: Release artifacts

on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install build
      - run: python -m build
      - name: Add reviewed bootstrap asset
        run: |
          cp scripts/install-dual.sh dist/install-dual.sh
          chmod 0755 dist/install-dual.sh
      - uses: anchore/sbom-action@v0
        with:
          path: .
          format: spdx-json
          output-file: dist/hermes-memory-router.spdx.json
      - name: Generate SHA-256 release manifest
        shell: bash
        run: |
          cd dist
          sha256sum \
            hermes_memory_router-*.whl \
            hermes_memory_router-*.tar.gz \
            install-dual.sh \
            hermes-memory-router.spdx.json \
            > SHA256SUMS-hermes-memory-router-0.2.0-beta.1.txt
      - name: Generate build provenance
        uses: actions/attest@v4
        with:
          subject-path: "dist/*"
      - uses: actions/upload-artifact@v4
        with:
          name: distributions
          path: dist/*
      - uses: softprops/action-gh-release@v2
        with:
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}
          files: dist/*

